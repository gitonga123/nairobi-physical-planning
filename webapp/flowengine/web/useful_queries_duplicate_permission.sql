INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 83,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 84,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 85,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 86,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 87,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 88,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 89,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 90,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 91,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 92,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 93,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;

INSERT INTO mf_guard_group_permission (group_id, permission_id)
SELECT 94,
    permission_id
FROM mf_guard_group_permission
WHERE group_id = 46;